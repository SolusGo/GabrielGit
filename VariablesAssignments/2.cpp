#include <iostream>
#include <iomanip>
#include <math.h>
using namespace std;

int main() {
	double value;
    cin >> value;
    cout << fixed << setprecision(1);
	/* Type your code here */
	cout << "Sqrt: "<< sqrt(value) << endl;
	return 0;
}